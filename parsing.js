























// ----------------- UNIX TIME -----------------

//var months_arr = ['Jan','Feb','Mar','Apr','May','Jun','Jul','Aug','Sep','Oct','Nov','Dec'];

// log date and time

/*
for(let i = 0; i < events.events.length; i++){
  let start = new Date(events.events[i]['start_time']);
  let end = new Date(events.events[i]['end_time']);
  console.log("event " + events.events[i].id + 
              "\nstart: " + start +
              "\nend: " + end 
  );
}
 

  let start = new Date(events.events[i]['start_time'] * 1000);
  console.log(
    months_arr[date.getMonth()] + ' ' + 
    start.getDate() + ' ' + 
    start.getFullYear() + ' at ' + 
    start.getHours() + ':' + 
    start.getMinutes().substr(-2) 
  );
*/
